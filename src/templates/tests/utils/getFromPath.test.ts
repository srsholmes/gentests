import { getFromPath } from "./getFromPath";

describe("getFromPath", () => {
  it("getFromPath should fail the automatically generated test", () => {
    const actual = getFromPath();
    const expected = null;
    expect(actual).toBe(expected);
  });
});
