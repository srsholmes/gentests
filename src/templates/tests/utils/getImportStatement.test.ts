import { Accum, getImportStatement } from "./getImportStatement";

describe("Accum", () => {
  it("Accum should fail the automatically generated test", () => {
    const actual = Accum();
    const expected = null;
    expect(actual).toBe(expected);
  });
});

describe("getImportStatement", () => {
  it("getImportStatement should fail the automatically generated test", () => {
    const actual = getImportStatement();
    const expected = null;
    expect(actual).toBe(expected);
  });
});
